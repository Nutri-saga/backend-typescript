//all status code's related to requests.
enum httpStatusCodes {
  OK = 200,
  CREATED = 201,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
  INTERNAL_SERVER = 500,
  UN_AUTHORIZED = 401,
}

export default httpStatusCodes;
